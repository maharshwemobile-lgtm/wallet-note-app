"use client";import{useEffect,useState}from"react";export default function SettingsClient(){const[d,setD]=useState<any>(null);const[msg,setMsg]=useState('');useEffect(()=>{fetch('/api/settings').then(r=>r.json()).then(setD)},[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rate:Number(f.get('rate')),multiplier:Number(f.get('multiplier'))})});setMsg(r.ok?'Settings saved':'Save failed');}if(!d)return <>Loading...</>;return <form onSubmit={submit} className="card max-w-xl space-y-4"><label className="block"><span className="mb-1 block font-medium">Your THB to MMK rate</span><input name="rate" type="number" defaultValue={d.thbToMmkRate||0} className="field"/></label><label className="block"><span className="mb-1 block font-medium">Your 3D payout multiplier</span><input name="multiplier" type="number" defaultValue={d.payoutMultiplier||500} className="field"/></label><button className="btn-primary">Save Settings</button>{msg&&<p>{msg}</p>}</form>}
