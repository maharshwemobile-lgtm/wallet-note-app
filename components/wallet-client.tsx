"use client"; import { useEffect,useState } from "react";
export default function WalletClient(){const[d,setD]=useState<any>(null);const load=()=>fetch('/api/wallet').then(r=>r.json()).then(setD);useEffect(()=>{void load()},[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await fetch('/api/wallet',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:f.get('type'),currency:f.get('currency'),amount:Number(f.get('amount')),note:f.get('note')})});(e.target as HTMLFormElement).reset();load();}if(!d)return <>Loading...</>;return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-2"><div className="card text-2xl font-bold">THB: ฿ {d.balances.THB.toLocaleString()}</div><div className="card text-2xl font-bold">MMK: K {d.balances.MMK.toLocaleString()}</div></div><form onSubmit={submit} className="card grid gap-3 md:grid-cols-5"><select name="type" className="field"><option value="CASH_IN">Cash In</option><option value="CASH_OUT">Cash Out</option></select><select name="currency" className="field"><option>THB</option><option>MMK</option></select><input name="amount" type="number" min="1" className="field" placeholder="Amount" required/><input name="note" className="field" placeholder="Note"/><button className="btn-primary">Submit Pending</button></form><div className="card overflow-auto"><table className="w-full text-left"><thead><tr className="border-b"><th className="p-2">Date</th><th>Type</th><th>Currency</th><th>Amount</th><th>Status</th></tr></thead><tbody>{d.rows.map((r:any)=><tr key={r.id} className="border-b"><td className="p-2">{new Date(r.createdAt).toLocaleString()}</td><td>{r.type}</td><td>{r.currency}</td><td>{Number(r.amount).toLocaleString()}</td><td>{r.status}</td></tr>)}</tbody></table></div></div>}
