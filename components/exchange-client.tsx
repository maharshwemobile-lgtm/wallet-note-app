"use client";import{useEffect,useState}from"react";export default function ExchangeClient(){const[d,setD]=useState<any>(null);const load=()=>fetch('/api/exchange').then(r=>r.json()).then(setD);useEffect(()=>{void load()},[]);if(!d)return <>Loading...</>;return <div className="space-y-6"><div className="card"><p>Current rate</p><p className="text-3xl font-bold">1 THB = {d.rate.toLocaleString()} MMK</p></div></div>}
