import { NextResponse } from "next/server"; import { z } from "zod"; import { readSession } from "@/lib/auth"; import { getBalances, recordWalletTransaction } from "@/lib/wallet"; import { getObjectRows } from "@/lib/sheets";
export async function GET(){const s=await readSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const [balances,rows]=await Promise.all([getBalances(s.userId),getObjectRows("WalletTransactions")]);return NextResponse.json({balances,rows:rows.filter(r=>r.userId===s.userId).reverse()});}
export async function POST(req:Request){const s=await readSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const p=z.object({type:z.enum(["CASH_IN","CASH_OUT"]),currency:z.enum(["THB","MMK"]),amount:z.number().positive(),note:z.string().optional()}).safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid data"},{status:400});await recordWalletTransaction({...p.data,userId:s.userId,status:"Pending"});return NextResponse.json({ok:true});}
